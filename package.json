{
  "name": "sintracode",
  "version": "1.0.0",
  "private": true,
  "bin": {
    "sintracode": "./run-sintracode"
  },
  "scripts": {
    "start": "./run-sintracode",
    "print": "./run-sintracode -p",
    "lint": "eslint . --ext .js,.ts",
    "test": "jest",
    "prepublishOnly": "npm run lint && npm test"
  },
  "jest": {
    "testPathIgnorePatterns": ["/sintracode/"],
    "testEnvironment": "node"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "husky": "^9.1.7",
    "jest": "^29.7.0"
  }
}
